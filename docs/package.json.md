# package.json.md

## 目的
- Vue + Three.js + Vite 構成の依存と実行コマンドを管理する。

## 主要スクリプト
- `dev`: Vite 開発サーバ起動。
- `build`: 本番ビルド。
- `preview`: ビルド結果確認。
- `test`: `tests/game.test.js` を実行。
- `typecheck`: `tsc --noEmit` を実行。
